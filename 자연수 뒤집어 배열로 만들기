def solution(n):
    n = list(str(n))
    n.sort(reverse=True)

    answer = n
    return list(map(int, n))

    # 테스트 통과불과 판정
# answer
def solution(n):
a = []
s = reversed(str(n))
for i in s:
    a.append(int(i))
return a
